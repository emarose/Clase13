let cadena = "javascript";
let uppercaseFirstLetter = cadena.charAt(0).toUpperCase(); //primer caracter a mayuscula
let string = cadena.slice(1); // resto del string
console.log(uppercaseFirstLetter+string);
